@using Orchard.Utility.Extensions
@{
    decimal? number = Model.ContentField.Value;
    string name = Model.ContentField.DisplayName;
}

@if (number.HasValue) {
    <span class="numeric-field numeric-field-@name.HtmlClassify()">
        <span class="value">(@(Model.Value))</span>
    </span>
}
